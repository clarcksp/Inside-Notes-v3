# =================================================================
#  Variáveis de Ambiente - Exemplo para Inside Notes v2.0
# =================================================================
#
# Copie este arquivo para .env na raiz do projeto e preencha com seus valores.
# O arquivo .env NUNCA deve ser enviado para o controle de versão (Git).

# --- Variáveis do Backend (lidas pelo docker-compose) ---

# Credenciais para o banco de dados PostgreSQL
# Estes valores são usados para criar o banco e para o backend se conectar.
PG_USER=inside_user
PG_PASSWORD=supersecretpassword
PG_DATABASE=inside_notes_db

# Segredo para assinatura dos tokens JWT (JSON Web Tokens)
# Use uma string longa, complexa e aleatória para segurança.
JWT_SECRET="um-segredo-jwt-muito-forte-e-longo-e-aleatorio"

# --- Variáveis do Frontend ---
# ATENÇÃO: A chave da API do Gemini é usada no código do frontend (services/geminiService.ts).
# Em uma aplicação real, expor a chave de API no lado do cliente é um risco de segurança.
# O ideal seria que as chamadas para a API Gemini fossem feitas através do seu backend.
# Para este projeto, assume-se que há um processo (não definido aqui) que
# disponibiliza esta variável no ambiente de execução do frontend.
API_KEY="SUA_CHAVE_DE_API_DO_GEMINI_AQUI"
